# PRODUCTION Environment Configuration
# Copy this file to .env and fill in your actual values
# Never commit .env file with real credentials - it's in .gitignore

# ===== EMAIL CONFIGURATION (SMTP Recommended) =====
# Production: Use SMTP server (zero cost, full control)
# Development: Use Gmail for convenience (100 emails/day free)
# See EMAIL_SETUP.md for detailed configuration

EMAIL_PROVIDER=smtp
# EMAIL_PROVIDER=gmail
# EMAIL_PROVIDER=sendgrid

# SMTP Configuration (Recommended for Production)
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@yourdomain.com
SMTP_PASSWORD=your-mail-server-password
EMAIL_FROM=noreply@yourdomain.com

# Gmail Configuration (Development Only - Limited to 100 emails/day)
# GMAIL_EMAIL=your-email@gmail.com
# GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# SendGrid Configuration (Optional - Free tier: 100 emails/day)
# SENDGRID_API_KEY=SG.your-api-key-here

# ===== APPLICATION CONFIGURATION =====
FRONTEND_URL=https://yourdomain.com
NODE_ENV=production

# ===== DATABASE CONFIGURATION =====
MONGO_URI=mongodb://your-mongodb-connection-string
# Or for local development:
# MONGO_URI=mongodb://localhost:27017/household

# ===== JWT AUTHENTICATION =====
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-very-long-random-secret-key-here

# ===== OPTIONAL: THIRD-PARTY SERVICES (For Phase 2+) =====
# PLAID_CLIENT_ID=your-plaid-client-id
# PLAID_SECRET=your-plaid-secret
# STRIPE_SECRET_KEY=your-stripe-secret-key
