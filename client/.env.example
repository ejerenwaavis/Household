# ─── LOCAL DEVELOPMENT ───────────────────────────────────────────────────────
# No VITE_API_URL needed locally! The app auto-detects the backend at
# http://localhost:4000/api based on window.location.hostname.
#
# Only set VITE_API_PORT if your backend runs on a non-default port:
# VITE_API_PORT=4000

# ─── PRODUCTION (Namecheap) ──────────────────────────────────────────────────
# Set this only in your production build environment.
# If not set, the app falls back to /api (Apache reverse proxy).
# VITE_API_URL=https://api.aceddivision.com/api
